export type SiteConfig = typeof siteConfig;

export const siteConfig = {
  name: "Life Value Tracker",
  description: "Determine your worth, completely factually and statistically",
  navItems: [
    {
      label: "Home",
      href: "/",
    },
  ],
};
